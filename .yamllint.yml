extends: default
ignore: |
  build/
  coverage/
  site/
  vendor/
rules:
  document-start: disable
  line-length:
    max: 140
    level: warning
  truthy:
    allowed-values: ["true", "false", "on", "off"]
